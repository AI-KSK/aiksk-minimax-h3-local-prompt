# v1.4 多参考增强与交叉验证修订报告

## 本次修复对象

v1.3已经建立预注册、多任务、多Seed、盲评和独立环境复现框架，但Ref2VA多参考输入仍缺少可执行的真实标签解析，尤其没有写透“视频配套音轨先占用Audio标签，独立音频随后编号”的官方ComfyUI实现顺序。

## v1.4核心修复

1. 逐行复核官方`MiniMaxH3ReferenceToVideo`源码和官方Ref提示词指南。
2. 新增`h3_refmap.py`与`build_ref2va_manifest.py`，从工作流实际连线生成`<Picture N>`、`<Video N>`、`<Audio N>`映射。
3. 明确`ref_video_audio_N`只负责与同尾号视频配对，端口尾号不等于提示词标签号。
4. 明确`<Audio N>`是参考来源，`(Sx)`是目标视频实际发声者。
5. 新增来源—职责—目标—保留模式—排除项关系图。
6. 新增图片/视频身份污染、视频音轨与独立音频争夺、重复音频和相反动作视频等冲突矩阵。
7. 将多参考标签准确率、职责隔离、音频来源正确性、说话者绑定、音画同步和属性泄漏纳入v1.3交叉验证体系。
8. 对官方节点与RunningHub/RH自定义节点划定边界：未读取自定义节点源码时不得默认复用官方标签顺序。

## 交叉验证与验收

- Python脚本全部通过编译检查。
- 自动测试：23项全部通过。
- 测试同时覆盖v1.4新增映射逻辑和v1.3保留的预注册、矩阵、盲评合并及独立环境复现关键路径。
- 静态PASS不等于生成效果更好；包内没有伪造任何MiniMax H3实际生成优胜结论。
