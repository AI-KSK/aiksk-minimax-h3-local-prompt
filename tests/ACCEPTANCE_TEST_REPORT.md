# v1.5 工具链验收报告

验收日期：2026-08-04。

## 自动测试

```text
Ran 23 tests
OK
```

覆盖范围：

- 视频配套音轨优先占用`<Audio N>`编号；
- 无配套音轨时独立音频从`<Audio 1>`开始；
- 两视频、两配套音轨、一个独立音频的`Audio 1/2/3`顺序；
- 端口序号存在空洞时提示词标签连续压缩；
- 孤立视频音轨无效；
- 纯音频Ref2VA拒绝；
- 官方上传R2V工作流实际为两图片、零视频、零音频；
- 标签越界、端口名误写、声音未绑定说话者、`retention_analysis`错误写入`(Sx)`；
- Ref2VA专项评分维度；
- 预注册Case×Seed×Profile矩阵；
- Prompt Profile缺失与自审语义锁拒绝；
- 多评审隔离与重复评审拒绝；
- 独立环境复现条件与`supported_replicated`判定。

这些自动测试验证的是规则、映射和实验工具链，不代表MiniMax H3实际生成质量已经完成交叉验证。真实质量结论仍需在用户自己的H3环境中进行多任务、多Seed、盲评和独立环境复现。
